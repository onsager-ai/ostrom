{
  "name": "@ostrom/constitution-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/doctor.ts --bundle --platform=node --target=node18 --format=esm --outfile=../dist/doctor.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
