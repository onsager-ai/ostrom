harness = "claude"
cli_version = "2.1.263"
capture_date = "2026-09-07"
exercises = "a 190253-scalar assistant text truncated at MAX_TEXT_SCALARS, with no result frame"

# Model: claude-haiku-4-5-20251001.
# Command: claude -p --output-format stream-json --verbose --model haiku;
# prompt supplied on stdin; 13 raw frames.
# Absolute paths were rewritten under /workspace. The over-bound narration is
# placeholder-shaped `lorem` text and was kept byte-identical so the bound is
# honest. The capture hit the capturing session's wall clock before a result.
# Its ASCII text exercises the scalar bound; ethogram's constructed astral
# tests exercise the code-point boundary. chreode-output.jsonl is prior art
# for diffing only, not the expectation.
