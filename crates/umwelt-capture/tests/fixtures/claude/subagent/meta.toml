harness = "claude"
cli_version = "2.1.263"
capture_date = "2026-09-07"
exercises = "init and repeated result frames, text, three paired tool calls, and parentToolUseId on sub-agent drafts"

# Model: claude-haiku-4-5-20251001.
# Command: claude -p --output-format stream-json --verbose --model haiku
#          --allowedTools Task Bash; prompt supplied on stdin; 37 raw frames.
# Narration and thinking were replaced at exact length. IDs and correlations
# are real; absolute paths were rewritten under /workspace.
# The Agent tool launches the sub-agent in 2.1.263. One session emitted two
# init and two result frames. Both results repeat the same session-cumulative
# cost, while turns and durations are per invocation. chreode-output.jsonl is
# prior art for diffing only, not the expectation.
